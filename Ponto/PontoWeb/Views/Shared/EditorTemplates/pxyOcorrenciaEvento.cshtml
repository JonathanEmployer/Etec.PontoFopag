@model Modelo.Proxy.pxyOcorrenciaEvento

<tr id="@Model.Id">
    <td>
        @Html.EditorFor(m => m.Selecionado, new { @class = "selectOco"})
        @Html.HiddenFor(m => m.Id)
        @Html.HiddenFor(m => m.Codigo)
        @Html.HiddenFor(m => m.Descricao)
    </td>
    <td>@Html.DisplayFor(m => m.Descricao)</td>
</tr>

